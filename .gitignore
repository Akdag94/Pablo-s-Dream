# Godot 4 editor and import caches
.godot/
/android/
/build/
/export/
*.translation

# Export artifacts
*.apk
*.aab
*.ipa
*.exe
*.pck
*.zip

# export_presets.cfg IS tracked — CI needs it to build. Signing identities are
# injected by Codemagic at build time and must never be committed here.

# OS noise
Thumbs.db
desktop.ini
.DS_Store
